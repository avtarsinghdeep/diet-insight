import { TestBed, inject } from '@angular/core/testing';
classify(name) %  > Guard;
from;
'./<%= dasherize(name) %>.guard';
describe('<%= classify(name) %>Guard', function () {
    beforeEach(function () {
        TestBed.configureTestingModule({
            providers: [, classify(name) %  > Guard]
        });
    });
    it('should ...', inject([, classify(name) %  > Guard], function (guard) { return ; },  %  > Guard), {
        expect: function (guard) { }, : .toBeTruthy()
    });
});
;
//# sourceMappingURL=__name@dasherize__.guard.spec.js.map